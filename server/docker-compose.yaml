version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: match_master_backend
    ports:
      - "3000:3000"
    env_file:
      - .env.development
    volumes:
      - .:/app
    command: npm run dev